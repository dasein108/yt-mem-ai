Extract highlights for the YouTube video: $ARGUMENTS

Use the `yt-mem-ai` MCP tools. Ensure it's ingested, then produce 3–8 highlights
as `MM:SS — label` with `watch?v=<id>&t=<start>s` deep links. Anchor every
timestamp with the `search` tool (use a hit whose `video_id` matches) — never
invent one. Persist via `save_summary`.
