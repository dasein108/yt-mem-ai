Summarize the YouTube video: $ARGUMENTS

Use the `yt-mem-ai` MCP tools. Ensure it's ingested (`fetch`, captions→whisper),
reuse a stored `summary` from `show` if present, then produce an executive
summary (2–4 sentences) + key bullets in the video's original language, and
persist with `save_summary`.
