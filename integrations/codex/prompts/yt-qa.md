Generate Q&A for the YouTube video: $ARGUMENTS

Use the `yt-mem-ai` MCP tools. Ensure it's ingested (`fetch`, captions→whisper),
then produce 3–6 question/answer pairs, each drawn strictly from the transcript.
