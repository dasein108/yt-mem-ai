Build today's subscriptions digest.$ARGUMENTS

Use the `yt-mem-ai` MCP tools: run `discover`, then `fetch_pending`, then for
each of the day's transcribed videos (`list_videos` status=transcribed) run the
summarize+highlights+Q&A analysis and `save_summary`. Compose `digests/<DATE>.md`
— an executive digest plus one section per video (summary, top highlights, 2–3
Q&A).
