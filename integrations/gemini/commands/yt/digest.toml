description = "Process the latest subscription uploads into a dated digest."
prompt = """
Build the subscriptions digest. {{args}}

Use the yt-mem-ai MCP tools: discover, then fetch_pending, then for each of the
day's transcribed videos (list_videos status=transcribed) run the
summarize+highlights+Q&A analysis and save_summary. Compose digests/<DATE>.md —
an executive digest plus one section per video.
"""
