---
description: Process the latest subscription uploads into a dated digest.
argument-hint: [YYYY-MM-DD]
---

Use the **yt** skill, scenario B (daily digest)$ARGUMENTS

Run `discover` + `fetch-pending`, analyze each of the day's transcribed videos,
and compose `digests/<DATE>.md` (executive digest + one section per video).
