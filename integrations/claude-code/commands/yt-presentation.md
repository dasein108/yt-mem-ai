---
description: Build a slide deck (slides/<id>.md) from a YouTube video.
argument-hint: <url-or-video-id>
---

Use the **yt-agent** skill, scenario A (presentation), for: $ARGUMENTS

Write a `---`-separated slide deck to `slides/<video_id>.md`: title slide, one
slide per theme (heading + 3–5 bullets + a notable quote with its `MM:SS`), and
a takeaways slide with the watch link.
