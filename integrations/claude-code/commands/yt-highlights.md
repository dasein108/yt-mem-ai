---
description: Extract timestamped, deep-linked highlights for a YouTube video.
argument-hint: <url-or-video-id>
---

Use the **yt** skill, scenario A (highlights), for: $ARGUMENTS

Produce 3–8 highlights as `MM:SS — label` with `watch?v=<id>&t=<start>s` deep
links. Anchor every timestamp with `yt-ai search` (or the search MCP tool) —
never invent one.
